{
	"name": "flights",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"prebuild": "node increment-version.js",
		"deploy": "wrangler deploy",
		"build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --target=es2022",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"esbuild": "^0.25.10",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.39.0"
	},
	"dependencies": {
		"prettier": "^3.6.2",
		"typegram": "^5.2.0"
	}
}
